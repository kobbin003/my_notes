URL.createObjectURL:

👉️ URL.createObjectURL is a method in JavaScript that is used to create a temporary URL for a 
specified Blob or File object. This method is often used in web applications to display or 
manipulate files that are selected or generated by the user.

 	🧑‍💻️ const tempURL = URL.createObjectURL(file); 
 	/* file uploaded from file type input. */

👉️ Use of Temporary URL: 
You can then use the generated URL in various ways within your web application, such as:

- Displaying an image or video in an <img> or <video> element by setting the src attribute to the generated URL.
- Playing audio using an <audio> element.
- Creating downloadable links for files using <a> elements with the href attribute set to the generated URL.
